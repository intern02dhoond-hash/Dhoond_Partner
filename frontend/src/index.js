/**
 * index.js
 * App entry point
 */

import { AppRegistry } from 'react-native';
import App from './App';

// NOTE: Replace 'DhoondPartnerApp' with your actual app name
// from app.json if using Expo, or from your native project config.
AppRegistry.registerComponent('DhoondPartnerApp', () => App);

export default App;
